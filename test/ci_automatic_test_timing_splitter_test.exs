defmodule CiAutomaticTestTimingSplitterTest do
  use ExUnit.Case
  doctest CiAutomaticTestTimingSplitter

  test "greets the world" do
    assert CiAutomaticTestTimingSplitter.hello() == :world
  end
end
